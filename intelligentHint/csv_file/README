These csv files are all generated from completeSE/Face Morphing.se

operation:

This folder contains the original operation of students.
The original SE file of this fold is in mockSE. They are generated by read_se.py. They are classified into two types: success, failure.
From mockSE files, we use mockdataProducer.py to imitate the operations of four kinds of students: mover(1), stopper(2), extremeMover(3), tinker(4).
Each type students have 25 mock data.

diff:
This folder contains the differientiate between two snapshots of operation of students.
